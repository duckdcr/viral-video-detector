interface:
  display_name: "Viral Video Detector"
  short_description: "Detect explainable energy-storage content outliers"
  default_prompt: "Use $viral-video-detector to scan configured markets for early energy-storage viral candidates and explain the strongest signals."
policy:
  allow_implicit_invocation: false
